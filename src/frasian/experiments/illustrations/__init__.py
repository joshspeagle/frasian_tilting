"""Per-method illustrative demo scripts.

Each registered method has an `<name>_demo.py` here that runs in `--smoke`
mode under CI to verify the method produces a figure end-to-end. Step 2 adds
the first demos (power_law_demo, waldo_demo, wald_demo).
"""
